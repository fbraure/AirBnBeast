<div class="container">

  <div class="profil">
    <%=image_tag(@offer.user.avatar_url, class:"avatar-bordered")%>
    <div class="profil-content">
      <h2><%= @seller %></h2>
      <p><%= @offer.user.description %></p>
    </div>
  </div>

  <div class="card-product">
    <%= image_tag(@offer.photo_url)%>
    <div class="card-product-infos">
      <h2><%= @offer.title %></h2>
      <p><%= @offer.description %></p>
      <p><%= @offer.date %></p>
      <div class="btn-book">
        <%= link_to "BOOK", offer_bookings_path(@offer), method: :POST, class: "btn btn-primary" %>
      </div>
      <h2 class="pricing"><%= @offer.price %>€</h2>
    </div>
  </div>
  <div class="d-flex justify-content-start my-4">
      <%= link_to "Back to offers", offers_path %>
  </div>

</div>
