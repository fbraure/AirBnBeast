<!--  Afficher le nom de current_user -->
<!--  Afficher la description de current_user -->
<!--  Afficher les offres créé par current_user -->
<% @offers.each do |offer| %>
  <div>
    <%= offer.user.first_name %> <!-- N'existe pas dans seed -->
    <%= offer.user.last_name %>
    <%= offer.user.email %> <!-- A enlever & Créer un champ description & avatar -->
  </div>
  <div>
    <%= offer.title %>
    <%= offer.description %>
    <%= offer.price %>
    <%= offer.date %>
  </div>
<% end %>
<!--  Afficher les offres achetés de current_user -->
